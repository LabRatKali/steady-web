# Steady website

Public marketing + download site for Steady (self-focus and parenting).

**Family dashboard:** `dashboard.html` talks to private `LabRatKali/steady-sync` via the GitHub Contents API (same encrypted paths as the Android app). Auth = fine-grained PAT stored in the browser — no separate cloud host.
